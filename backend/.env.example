# Redis (Action Cable, Sidekiq). Override in production.
REDIS_URL=redis://localhost:6379/7

# Comma-separated browser origins allowed by CORS (cookie auth). Override in production.
FRONTEND_ORIGINS=http://localhost:3000
